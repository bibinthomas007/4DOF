
#Referance
https://wiki.ros.org/rosserial_arduino/Tutorials/Arduino%20IDE%20Setup
#using UART Arduino will be a full fledged ROS node which can directly publish and subscribe to ROS messages, publish TF transforms, and get the ROS system time.
#make use ROS directly with the Arduino IDE (Installing from Binary code given below)
sudo apt-get install ros-noetic-rosserial-arduino
# rosserial provides a ROS communication protocol that works over your Arduino's UART
sudo apt-get install ros-noetic-rosserial
#now we have installed the necessary packages
#In arduino IDE add ros_lib library
